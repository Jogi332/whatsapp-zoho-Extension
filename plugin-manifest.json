{
  "_comment": "UNVALIDATED - see plan Phase C. This was hand-authored (the `zet` CLI is not available/authenticated in this environment) against Zoho's documented Extension manifest shape, not generated by `zet init` and not run through `zet validate` against a real Zoho account. Treat every field here as a best-effort draft to be corrected once `zet validate`/`zet pack` can actually run. Do not submit for review without that step.",

  "plugin-manifest": {
    "version": "1.0",

    "extension": {
      "name": "whatsyoo-whatsapp",
      "display_name": "Whatsyoo - WhatsApp for Zoho CRM",
      "version": "1.0.0",
      "description": "Send and receive WhatsApp messages from inside Zoho CRM Leads, powered by Engati live chat.",
      "_comment_business_fields": "Placeholders - see plan Phase G, these are business/legal artifacts this migration cannot produce on its own.",
      "support-mail": "REPLACE-WITH-SUPPORT-EMAIL",
      "privacy-policy-url": "REPLACE-WITH-PRIVACY-POLICY-URL",
      "icons": {
        "_comment": "Zoho requires extension icons (typically 32x32 and a larger listing image) - not present anywhere in this repo yet (diagnostic-assets/ is unrelated). Needs real design assets before packaging.",
        "small": "REPLACE-WITH-ICON-PATH"
      }
    },

    "services": ["crm"],

    "scopes": [
      "ZohoCRM.modules.leads.ALL",
      "ZohoCRM.modules.tasks.ALL",
      "ZohoCRM.signals.ALL",
      "ZohoCRM.settings.variables.READ"
    ],

    "_comment_functions": "See deluge/leadSync.dg (new) - a REST-API-triggered Deluge Function must also be declared/authored here via Sigma (Automate > Functions, 'Invoke as REST API': Yes), NOT deployable from this Node/Catalyst repo. It replaces functions/liveChatWebhook/crmLeads.js's OAuth-based CRM write for the shared-backend path - confirmed by Zoho Marketplace support since Sigma doesn't support Connections for CRM extensions. Not yet wired into this manifest's schema (unconfirmed field name for declaring a bundled Function - correct once zet/Sigma access is available).",

    "components": {
      "widgets": [
        {
          "_comment": "Reuses the existing externally-hosted widget as-is - see plan Phase F, no change to how the widget itself is served.",
          "name": "whatsyoo_panel",
          "display_name": "WhatsApp",
          "location": "related_list",
          "module": "Leads",
          "url": "https://jogi332.github.io/whatsapp-zoho-widget/index.html"
        },
        {
          "_comment": "New in this migration - see plan Phase B. Not yet built (settings.html/settings.js don't exist yet); this entry documents the intended registration so Phase B's build has a manifest slot to land in.",
          "name": "whatsyoo_settings",
          "display_name": "Whatsyoo Settings",
          "location": "system_settings",
          "url": "https://jogi332.github.io/whatsapp-zoho-widget/settings.html"
        }
      ]
    },

    "plugin-execution": {
      "_comment": "Install/uninstall/upgrade lifecycle hooks - see plan Phase C and functions/extensionLifecycle/. Exact field names/shape (event names, payload) are UNCONFIRMED against Zoho's real Extension SDK - functions/extensionLifecycle/index.js is written defensively (logs the raw payload, tries several plausible field names) pending that confirmation.",
      "install-url": "https://REPLACE-WITH-SHARED-PROJECT.catalystserverless.in/server/extensionLifecycle/",
      "uninstall-url": "https://REPLACE-WITH-SHARED-PROJECT.catalystserverless.in/server/extensionLifecycle/",
      "upgrade-url": "https://REPLACE-WITH-SHARED-PROJECT.catalystserverless.in/server/extensionLifecycle/"
    }
  }
}
